# Ecotaxa login
# Cut-paste lines 1:4, replace with appropriate credentials, and save as Ecotaxa_API_pw.yalm (file on .gitignore)
ecotaxa_user : 'your Ecotaxa email login'
ecotaxa_pass : 'your Ecotaxa password'


# GIT repository for storage
git_dir : '~/GIT/PSSdb/'

#raw subdirectory
raw_dir : '~/GIT/PSSdb/raw/'

#standardized raw subdirectory
standardized_raw_subdir : 'raw_standardized'

# Ecotaxa exported files storage sub-direcotry :
Ecotaxa_subdir : 'ecotaxa'

# Ecopart exported files storage sub-direcotry:
Ecopart_subdir : 'ecopart'

#IFCB dashboard directory:
IFCB_dir : 'IFCB_dashboard_downloads'

# General data storage sub-directory
dataset_subdir : 'raw'

#standadrized datasets storage sub-subdirectory
standardized_subdir : 'raw/raw_standardized'

#binned datasets storage sub-subdirectory
gridded_subdir : 'gridded_data'

# Path to project_list_all.xlsx
proj_list : 'raw/project_list_all.xlsx'

# Numeric id of project to download
# you should have appropriate access rights to it:
# - either the project should be visible to all
# - or you should be explicitly registered to it, as viewer at least
proj_id : 3302
